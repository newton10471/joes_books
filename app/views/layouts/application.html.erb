<!DOCTYPE html>
<html>
	<head>
	  <title>JoesBooks</title>
	  <%= stylesheet_link_tag    "application", media: "all" %>
	  <%= javascript_include_tag "application" %>
	  <%= csrf_meta_tags %>
	  <!--[if lt IE 9]>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
		<![endif]-->
	</head>

	<body>
		<header class="navbar navbar-fixed-top">
			<div class="navbar-inner">
				<div class="container">
					<%= link_to "joe\'s books", '#', id: "logo" %>
					
					<nav>
						<ul class="nav pull-right">
							<% if user_signed_in? %>
								<%= link_to 'Home', user_books_path(current_user) %>
								<%= link_to 'Vote', new_user_vote_path(current_user) %>
								<%= link_to 'All books', all_books_index_path %>
								<%= link_to 'All votes', all_votes_path %>
								<% if current_user.admin? %>
									<%= link_to 'Admin page', static_pages_admin_home_path %>
								<% end %>
								<%= link_to 'Sign out', destroy_user_session_path, method: "delete" %>
								Signed in as user 
								<%= current_user.email %> 
							<% end %>
						</ul>
					</nav>
				</div>
			</div>
		</header>
		<div class="container">
			<p class="notice"><%= notice %></p>
			<p class="alert"><%= alert %></p>

			<%= yield %>

			</p>
		</div>
	</body>
</html>
