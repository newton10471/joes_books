<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @book.title %>
</p>

<p>
  <b>Author:</b>
  <%= @book.author %>
</p>

<p>
  <b>Amazon link:</b>
  <%= link_to  raw(amazon_pic_url(@book.asin)) %>
</p>

<% if @book.user_id == current_user.id %>
	<%= link_to 'Edit', edit_user_book_path(current_user, @book) %> | 
<% end %>
<%= link_to 'Back', all_books_index_path %>
