<%= form_for @comment, :url => user_book_path(current_user, Book.find(params[:book_id])) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :text, placeholder: "Compose new comment here ..." %>
  </div>
  
  # this passes along the :book_id param we received from the linking page
  <%= hidden_field_tag(:book_id, params[:book_id]) %>  

  <div class="actions">
    <%= f.submit "Post Comment", class: "btn btn-large btn-primary" %>
  </div>
<% end %>
